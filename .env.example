# Copy to .env (same folder). Server reads this at boot (see server/src/env.ts).
# Without these the server runs in DEV MODE: no JWT verification on the
# WebSocket handshake and no match ledger writes. See supabase/schema.sql.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
