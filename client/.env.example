# Copy to client/.env. Without these the client runs in DEV MODE: no login
# screen, progress stays in localStorage. With them, login is mandatory and
# progress syncs to your Supabase project (supabase/schema.sql).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
