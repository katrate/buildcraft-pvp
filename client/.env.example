# Copy to client/.env. Accounts are mandatory: without these keys the app shows
# a "Supabase is not configured" setup screen and refuses to run anonymously.
# Progress syncs to your Supabase project (run supabase/schema.sql first).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
